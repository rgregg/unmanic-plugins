An improved Plex notification plugin that triggers a targeted scan of only the
specific folder containing the processed file, rather than scanning the entire
Plex library.

Requires a path mapping if Unmanic and Plex see the media files under different
mount paths (e.g. Unmanic uses `/library/movies/...` while Plex uses
`/data/movies/...`).
